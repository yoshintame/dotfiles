# yaml-language-server: $schema=https://creativeprojects.github.io/resticprofile/jsonschema/config.json

version: "2"

global:
  default-command: snapshots
  scheduler: crond
  restic-binary: /opt/homebrew/bin/restic

groups:
  all:
    profiles:
      - dev
      - home
  maintenance:
    profiles:
      - local-ops
      - b2-ops
  check:
    profiles:
      - local-ops
      - b2-ops

profiles:
  default:
    inherit: local

  base:
    initialize: true
    password-file: "{{ .Env.HOME }}/.config/resticprofile/repo.key"
    backup:
      exclude:
        - "**/node_modules"
        - "**/.venv"
        - "**/.DS_Store"
        - "**/Backups/restic/**"
        - "**/.config/resticprofile/*.key"
      exclude-caches: true
      one-file-system: false
      no-error-on-warning: true
      run-after:
        # Telegram success notification
        - >-
          /opt/homebrew/bin/apprise -e
          -c "{{ .Env.HOME }}/.config/resticprofile/apprise.yaml"
          --tag backup-success
          --tag "hc-{{ .Profile.Name }}-backup-success"
          -t "🟢 Backup Success: {{ .Profile.Name }}"
          -b "Host: $(hostname -s)\nProfile: {{ .Profile.Name }}\nOperation: backup\nTime: $(date '+%Y-%m-%d %H:%M:%S')"
          -vv
      run-after-fail:
        # Telegram + email failure notification + HC.io fail ping
        - >-
          /opt/homebrew/bin/apprise -e
          -c "{{ .Env.HOME }}/.config/resticprofile/apprise.yaml"
          --tag backup-fail
          --tag "hc-{{ .Profile.Name }}-backup-fail"
          -t "🔴 Backup FAILED: {{ .Profile.Name }}"
          -b "Host: $(hostname -s)\nProfile: {{ .Profile.Name }}\nOperation: backup\nTime: $(date '+%Y-%m-%d %H:%M:%S')\nError: $ERROR_MESSAGE"
          -vv

    copy:
      run-after:
        - >-
          /opt/homebrew/bin/apprise -e
          -c "{{ .Env.HOME }}/.config/resticprofile/apprise.yaml"
          --tag backup-success
          --tag "hc-{{ .Profile.Name }}-success"
          -t "🟢 Copy Success: {{ .Profile.Name }}"
          -b "Host: $(hostname -s)\nProfile: {{ .Profile.Name }}\nOperation: copy\nTime: $(date '+%Y-%m-%d %H:%M:%S')"
          -vv
      run-after-fail:
        - >-
          /opt/homebrew/bin/apprise -e
          -c "{{ .Env.HOME }}/.config/resticprofile/apprise.yaml"
          --tag backup-fail
          --tag "hc-{{ .Profile.Name }}-fail"
          -t "🔴 Copy FAILED: {{ .Profile.Name }}"
          -b "Host: $(hostname -s)\nProfile: {{ .Profile.Name }}\nOperation: copy\nTime: $(date '+%Y-%m-%d %H:%M:%S')\nError: $ERROR_MESSAGE"
          -vv

    forget:
      run-after:
        - >-
          /opt/homebrew/bin/apprise -e
          -c "{{ .Env.HOME }}/.config/resticprofile/apprise.yaml"
          --tag backup-success
          --tag "hc-{{ .Profile.Name }}-success"
          -t "🟢 Forget/Prune Success: {{ .Profile.Name }}"
          -b "Host: $(hostname -s)\nProfile: {{ .Profile.Name }}\nOperation: forget\nTime: $(date '+%Y-%m-%d %H:%M:%S')"
          -vv
      run-after-fail:
        - >-
          /opt/homebrew/bin/apprise -e
          -c "{{ .Env.HOME }}/.config/resticprofile/apprise.yaml"
          --tag backup-fail
          --tag "hc-{{ .Profile.Name }}-fail"
          -t "🔴 Forget/Prune FAILED: {{ .Profile.Name }}"
          -b "Host: $(hostname -s)\nProfile: {{ .Profile.Name }}\nOperation: forget\nTime: $(date '+%Y-%m-%d %H:%M:%S')\nError: $ERROR_MESSAGE"
          -vv

    check:
      run-after:
        - >-
          /opt/homebrew/bin/apprise -e
          -c "{{ .Env.HOME }}/.config/resticprofile/apprise.yaml"
          --tag backup-success
          -t "🟢 Check Success: {{ .Profile.Name }}"
          -b "Host: $(hostname -s)\nProfile: {{ .Profile.Name }}\nOperation: check\nTime: $(date '+%Y-%m-%d %H:%M:%S')"
          -vv
      run-after-fail:
        - >-
          /opt/homebrew/bin/apprise -e
          -c "{{ .Env.HOME }}/.config/resticprofile/apprise.yaml"
          --tag backup-fail
          -t "🔴 Check FAILED: {{ .Profile.Name }}"
          -b "Host: $(hostname -s)\nProfile: {{ .Profile.Name }}\nOperation: check\nTime: $(date '+%Y-%m-%d %H:%M:%S')\nError: $ERROR_MESSAGE"
          -vv

  # --- Local repository (hub for all backups) ---
  local:
    inherit: base
    repository: "local:{{ .Env.HOME }}/Backups/restic/main"
    lock: "/tmp/resticprofile-local.lock"
    run-before:
      - "mkdir -p {{ .Env.HOME }}/Backups/restic"

  # --- B2 repository (offsite copy); needs B2_ACCOUNT_ID, B2_ACCOUNT_KEY ---
  b2:
    inherit: base
    env-file: "{{ .Env.HOME }}/.config/resticprofile/b2.env"
    repository: "b2:yoshintame:restic/main"
    lock: "/tmp/resticprofile-b2.lock"

  dev:
    inherit: local
    backup:
      source-base: "{{ .Env.HOME }}/Development"
      source:
        - "."
      tag:
        - "{{ .Profile.Name }}"
      schedule: "*:00,30"
      schedule-permission: user
      schedule-log: "{{ .Env.HOME }}/.local/share/resticprofile/logs/resticprofile-dev.log"

  home:
    inherit: local
    backup:
      source-base: "{{ .Env.HOME }}"
      source:
        - ".ssh"
        - ".config"
        - ".dotfiles"
        - "Documents"
        - "Pictures"
        - "remnote"
        - "Backups"
        - ".gnupg"
        - ".zsh_history"
        - ".bash_history"
        - ".history.db"
        - ".local/share/fish/fish_history"
      tag:
        - home
      schedule: "2:00"
      schedule-permission: user
      schedule-log: "{{ .Env.HOME }}/.local/share/resticprofile/logs/resticprofile-home.log"

  local-to-b2:
    inherit: local
    env-file: "{{ .Env.HOME }}/.config/resticprofile/b2.env"
    copy:
      initialize: true
      initialize-copy-chunker-params: true
      repository: "b2:yoshintame:restic/main"
      password-file: "{{ .Env.HOME }}/.config/resticprofile/repo.key"
      schedule: "0,3,6,9,12,15,18,21:00"
      schedule-permission: user
      schedule-log: "{{ .Env.HOME }}/.local/share/resticprofile/logs/resticprofile-local-to-b2.log"

  local-ops:
    inherit: local
    forget:
      group-by: "host,tags"
      keep-within: 1d
      keep-daily: 14
      keep-weekly: 8
      keep-monthly: 6
      prune: true
      host: true
      tag: true
      schedule: "sun 4:00"
      schedule-permission: user
      schedule-log: "{{ .Env.HOME }}/.local/share/resticprofile/logs/resticprofile-local-ops-forget.log"
    check:
      schedule: "*-*-01 6:00"
      schedule-permission: user
      schedule-log: "{{ .Env.HOME }}/.local/share/resticprofile/logs/resticprofile-local-ops-check.log"

  b2-ops:
    inherit: b2
    forget:
      group-by: "host,tags"
      keep-daily: 30
      keep-weekly: 12
      keep-monthly: 12
      keep-yearly: 3
      prune: true
      host: true
      tag: true
      schedule: "sun 5:00"
      schedule-permission: user
      schedule-log: "{{ .Env.HOME }}/.local/share/resticprofile/logs/resticprofile-b2-ops-forget.log"
    check:
      read-data-subset: "5%"
      schedule: "*-*-01 7:00"
      schedule-permission: user
      schedule-log: "{{ .Env.HOME }}/.local/share/resticprofile/logs/resticprofile-b2-ops-check.log"
